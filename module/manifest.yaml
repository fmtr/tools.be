name: tools

autoexec:
  import: false
  .channels:
    development:
      import: true

patches:
  - .channels:
      development:
        .type: Version
        path: tools_version.be
        pattern: \bvar\s+VERSION\s*=\s*'{version}'